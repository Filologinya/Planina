#pragma once

typedef struct {
  char* exe_path;
  char* res_path;
  char* config_path;
} ResourceManager;

// (1-2) Инициализация и очистка ресурсов

// (3) прочитать png картинку
// (4) прочитать код шейдер программы из файла
